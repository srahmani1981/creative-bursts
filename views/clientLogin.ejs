<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/style.css">
  <title><%= title %></title>
</head>
<body>
  <div id="spinner" class="loader-container">
    <div class="loader"></div>
  </div>
  <header>
    <h1>Lacey Rahmani Photography</h1>
    <nav>
      <ul>
        <li><a href="/">Home</a></li>|
        <li><a href="/portfolio">Portfolio</a></li>|
    <li><a href ="/contact">Contact<a></li>|
        <!-- Additional navigation items can be added here -->
        <li><a href="/clientLogin">Client Login</a></li>
      </ul>
    </nav>
  </header>
  <section id="gallery">
    <!-- Portfolio images will go here -->
  </section>

  <div class="login-container">
    <h2>Client Login</h2>
    <form method="POST" action="/clientLogin">
      <div class="form-group">
        <label for="username">Username:</label>
        <input type="text" id="username" name="username" required>
      </div>
      <div class="form-group">
        <label for="password">Password:</label>
        <input type="password" id="password" name="password" required>
      </div>
      <button type="submit">Login</button>
    </form>
  </div>
<script>
  document.getElementById('loginForm').addEventListener('submit', function(e) {
      e.preventDefault();
      // Perform your "fake" check here. For now, just redirect.
      window.location.href = '/fakeDashboard'; // Redirect to a fake dashboard page.
  });
</script>
  
  <script>
    document.addEventListener("DOMContentLoaded", function() {
      const spinner = document.getElementById('spinner');
      
      // Hide spinner on page load
      window.addEventListener('load', function() {
        spinner.style.display = 'none';
      });
  
      // Show spinner on link click
      document.querySelectorAll('a').forEach(item => {
        item.addEventListener('click', function(e) {
          spinner.style.display = 'flex';
    // Delay navigation and hide spinner
    setTimeout(() => {
            window.location.href = href; // Redirect after the delay
          }, 100000); // Delay duration in milliseconds (e.g., 2000ms = 2 seconds)
  
        });
      });
    });
  </script>
  <footer>
    <p>&copy; <%= new Date().getFullYear() %> Lacey Rahmani Photography</p>
  </footer>

</body>
</html>

